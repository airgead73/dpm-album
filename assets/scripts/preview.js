const container = document.getElementById('photoContainer');
const tag = document.getElementById('photo');

const init = ($container, $tag) => {

  console.log('container:', $container);
  console.log('tag:', $tag);

}

if(container && tag) {
  init(container, tag);
}